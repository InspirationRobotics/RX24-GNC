from setuptools import setup, find_packages

setup(
    name='mission_core',
    version='0.1',
    description='Core mission package for Inspiration RobotX',
    author='Eesh Vij, Carson Rae',
    packages=find_packages(include=['mission_core'])
)